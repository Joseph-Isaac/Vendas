<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.planejamento.josephisaac.weudesmonteirovendas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovasAtividades.Clientes" />
        <activity android:name=".NovasAtividades.Produtos" />
        <activity android:name=".NovasAtividades.Vendas" />
        <activity android:name=".NovasAtividades.ClientesNovo" />
        <activity android:name=".NovasAtividades.ClientesEditar" />
        <activity android:name=".NovasAtividades.ProdutosNovo" />
        <activity android:name=".NovasAtividades.ProdutosEditar" />
        <activity android:name=".NovasAtividades.VendasNovoCliente" />
        <activity android:name=".NovasAtividades.VendasNovoTotal" />
        <activity android:name=".NovasAtividades.VendasNovoProduto" />
        <activity android:name=".NovasAtividades.VendasNovoDetalheDaVenda" />
        <activity android:name=".NovasAtividades.VendasEditar"></activity>
    </application>

</manifest>